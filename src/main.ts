import { createApp } from 'vue';
import App from './App.vue';
// import { squareDirective } from './helper';

const app = createApp(App);
// app.config.performance = true;
app.mount('#app');
// app.directive('square', squareDirective);
